\section{Что уже измерено}
\label{sec:neg}

Это тоже результат, и он объясняет, почему решение выглядит так, а не проще. Раздел начинался как
перечень того, что не работает; последний подраздел добавлен позже и описывает единственную попытку,
которая частично удалась, поэтому заголовок пришлось поправить.

Если скрининг настолько информативен, зачем городить совместное правдоподобие? Нельзя ли подключить
его в лоб? Проверены три способа.

\begin{table}[htbp]\centering\small
\begin{tabularx}{\textwidth}{@{}Xrrrl@{}}
\toprule
Подход & ST-RAE & $\Delta$ к своей базе & $\rho$ & $\Delta\rho$ \\
\midrule
Скрининг как признак, в лоб & 0.921 & $+0.138$ & 0.566 & $+0.009$ \\
То же с честной вложенной схемой & 0.788 & $0.000$ & 0.559 & $+0.006$ \\
Два этапа: скрининг, затем пересчёт & 1.006 & $+0.212$ & 0.561 & $+0.010$ \\
Смесь двух маршрутов пополам & 0.803 & $+0.009$ & 0.587 & $+0.036$ \\
\bottomrule
\end{tabularx}
\caption{Разности считаются от базовой линии \emph{того же прогона}: 0.783, 0.788, 0.794, 0.794.
Расходятся они не случайно --- это три разных скрипта с тремя разными конфигурациями, см. ниже, ---
поэтому сравнивать строки между собой по абсолютной величине нельзя, только по разностям.}
\label{tab:neg}
\end{table}

\emph{Почему базовые линии не совпадают.} Три скрипта, давшие эту таблицу, строят разбиение по
фингерпринту в 1024 бита, тогда как абляционная сетка и все остальные числа документа --- по 2048.
При 1024 битах кластеризация даёт 4694 кластера против 4703, и вектор фолдов другой: дайджест
\texttt{d7b7ca15} против \texttt{2d93c198}. Поэтому ни одно число этой таблицы не сопоставимо
с 0.767 из раздела~\ref{sec:proto}: различаются не только модели, но и выборки, на которых
они отложены.

Между собой фолды у трёх скриптов совпадают, и расхождение базовых линий объясняется целиком
конфигурацией: шесть физико-химических признаков против восьми даёт 0.783 против 0.788, а
250 итераций бустинга против 200 --- 0.788 против 0.794. Оба различия детерминированы и
воспроизводятся при повторном запуске точно. Разброс в 0.011 --- это не шум прогона, а три
разные модели на общих фолдах.

\emph{Скрининг как признак, в лоб.} Обучаем вспомогательную модель предсказывать $\log_2$fc, её выход
подаём как признак в основную. Ошибка --- вспомогательный признак посчитан моделью, для которой
обучающие строки основной модели были внутренними. На них он точнее, чем окажется на отложенных,
основная модель учится на него опираться, и опора рассыпается там, где её проверяют: 0.783 против
0.921. На честной вложенной схеме выигрыш ровно нулевой. Разница между 0.783 и 0.921 целиком
артефакт процедуры.

\emph{Двухэтапная инверсия.} Предсказать $\log_2$fc, потом перевести в $\pic$ монотонной калибровкой.
Обязана выдать число там, где правдоподобие плоское: в зонах насыщения из раздела~\ref{sec:cens} калибровка
не определена, и любое значение оттуда одинаково согласуется с показанием. Результат: абсолютная шкала
разваливается, а ранговая информация в среднем сохраняется --- но только в среднем, см. ниже.

\emph{Смесь.} Полусумма двух маршрутов. Стоит $+0.009$ ST-RAE и покупает $+0.036$ Спирмена, причём ранг
растёт на всех четырёх ферментах. Это уже почти приемлемый обмен, но он не решает задачу, а балансирует
между двумя плохими решениями.

Общий вывод: информация в скрининге есть, но «ранг улучшается всегда» --- верно только для макро. Поферментно двухэтапная инверсия ранг роняет, причём на том ферменте, вокруг которого построен весь документ: на 2D6 Спирмен идёт с 0.398 до 0.362, на 3A4 с 0.749 до 0.747, и держится макро только за счёт 1A2 ($+0.048$) и 2C9 ($+0.028$). Смесь двух маршрутов --- единственный из трёх вариантов,
который поднимает ранг на всех четырёх. Информация в скрининге, таким образом, есть, и вся проблема
в том, как перевести её в абсолютную шкалу $\pic$, не потеряв ранг там, где он нужнее всего. Этим,
по замыслу, занимается совместное правдоподобие через общую латентную кривую.

\subsection{Совместное правдоподобие: первый запуск}

Написанное выше --- это то, что можно было сказать до того, как строка 5 абляционной сетки
была запущена. Теперь она запущена, и предыдущий вывод требует существенного уточнения.
Подраздел длинный, поэтому сначала о том, что в нём вообще происходит.

\emph{Задача.} У нас есть два сорта измерений. Полная кривая доза--эффект дорога, её сняли
не для всех соединений, и именно она даёт $\pic$ --- то, что просят предсказать. Скрининг в
одной концентрации дёшев, его сняли гораздо шире, и он даёт одно число $\log_2$fc, косвенно
связанное с активностью. Вопрос: можно ли дешёвыми измерениями помочь предсказанию дорогой
величины?

\emph{Почему не подать скрининг просто как признак.} В тестовом наборе его нет вовсе ---
только имя и SMILES. Модель, которой на входе нужно показание скрининга, неотправляема в
принципе. Значит скрининг может влиять только на \emph{обучение}, а к моменту предсказания
исчезать.

\emph{Конструкция.} Общий ствол --- слой, который строит внутреннее представление молекулы,
--- и две головы поверх него: одна предсказывает $\pic$, другая $\log_2$fc. Обучаются они
вместе, поэтому градиент от скрининговой головы формирует общее представление, а на выводе
эта голова просто не вызывается. Ручка $\lambda_{\text{scr}}$ задаёт, насколько сильно
слагаемое скрининга участвует в обучении: при нуле оно не участвует вовсе.

Контрольная рука --- та же самая сеть при $\lambda_{\text{scr}} = 0$. Обе головы есть и там,
и там, число параметров одинаково, а начальные веса засеяны по паре (сид разбиения, номер
фолда) и \emph{не} по $\lambda$. То есть две руки стартуют из буквально одинаковых весов, и
единственное, чем они отличаются, --- присутствие скрининга в функции потерь. Это и делает
сравнение сравнением канала, а не двух разных сетей.

\emph{Как эти варианты называются дальше.} Вариант модели в эксперименте здесь и ниже
называется \emph{рукой} --- не путать с \emph{плечом} пробы (прямым или после преинкубации,
раздел~\ref{sec:data}): два соседних анатомических слова обозначают совершенно разные вещи.
Рук в этом подразделе две. \emph{Двухголовая} --- та, где у скрининга своя двухголовая рука.
\emph{Одноголовая} --- та, где отдельной головы нет и предсказание скрининга вычисляется из
активности через заранее подогнанную кривую прибора (в коде она называется \texttt{calibrated},
и это название неудачно: оно читается как «после перекалибровки», а означает ровно обратное ---
калибровка в неё зашита заранее и никакой перекалибровки там нет).

Что канал даёт, тоже стоит назвать точно. Он даёт не лишние \emph{строки} --- все 4905
молекул и так имеют хотя бы одну метку $\pic$, --- а лишние \emph{колонки}: для конкретного
фермента появляется сигнал с тех соединений, где его кривую не мерили, а скрин снимали. По
ферментам объём supervision вырастает в два-три раза.

\emph{По макро знак не держится.} «Сид» здесь --- это способ нарезать выборку на пять
частей для перекрёстной проверки; сидов четыре, то есть четыре независимые нарезки. Так вот,
при $\lambda_{\text{scr}} = 3$ канал на одном сиде заметно ухудшает результат, на двух
улучшает, на одном не меняет. Ответ на вопрос «помогает ли канал» зависит от того, какую
нарезку взять, и одно разбиение дало бы уверенный ответ --- любой из двух. Это ровно тот
случай, ради которого в этом репозитории и заведено правило четырёх сидов.

(В предыдущей версии этого абзаца стояли три числа, взятые из двух разных строк дозовой
кривой --- часть при $\lambda = 3$, часть при $\lambda = 1$. Каждая строка сама по себе была
верна, фраза, составленная из обеих, --- нет.)

Поферментно, однако, эффект упорядочен. Усреднив по четырём сидам,
получаем: на CYP3A4 и CYP2C9 канал помогает, на CYP1A2 вредит, на CYP2D6 вредит сильнее
всего. Этот порядок в точности повторяет то, насколько скрининг вообще связан с $\pic$ у
данного фермента: сильнее всего у 3A4, слабее всего у 2D6. Читается это так --- канал
помогает ровно настолько, насколько дешёвое измерение вообще что-то говорит о дорогом, и
вредит там, где не говорит почти ничего. Худшим оказывается CYP2D6, тот самый фермент,
вокруг трудности которого построен весь документ.

\emph{Насколько этому верить --- и ответ оказался «гораздо меньше, чем хотелось».} Ферментов
четыре, и совпадение порядка из четырёх элементов случайно происходит в одном случае из
двенадцати. Это один бит свидетельства, а не закон. Хуже того, у двухголовой руки этот
идеальный порядок держится на \emph{одном прогоне}: по сидам эффект на 2D6 равен
$+0.609$, $-0.040$, $-0.121$, $+0.034$, то есть первый сид даёт почти всё, а без него порядок
разваливается ($+0.400$ вместо $-1.000$). И пара 2C9\,/\,3A4 не различима вовсе --- разность
между ними 0.002 при ошибке среднего вчетверо большей. Из четырёхточечного порядка в этой
руке измерено ровно одно сравнение: 1A2 против пары \{2C9, 3A4\}.

У одноголовой руки порядок, наоборот, устойчив: он держится и по медиане, и без первого
сида. Дальше в подразделе речь идёт в основном о ней.

Развилка «ранг вверх, шкала вниз» оказалась не настоящей, но прежде чем показывать почему,
нужно различить две
вещи, которые дальше встречаются постоянно. Модель может правильно расставить молекулы
\emph{по порядку} активности и при этом систематически ошибаться в самих \emph{числах} ---
например, сжимать или растягивать шкалу. Первое называют ранговой информацией, второе ---
шкалой. Метрика конкурса смотрит на числа, поэтому испорченная шкала бьёт по результату, даже
если порядок безупречен.

Измерение показало, что канал портит именно шкалу, и портит \emph{монотонно}: порядок молекул
при включении канала почти не меняется. А монотонная порча снимается монотонным же
преобразованием. Изотоническая калибровка --- это как раз оно: по обучающим фолдам строится
неубывающая ступенчатая кривая, переводящая предсказание в лучшую оценку истины, и
применяется к отложенному фолду. Она не может переставить молекулы местами, она только
перерисовывает шкалу.

\begin{table}[htbp]\centering\small
\begin{tabularx}{0.86\textwidth}{@{}Xrrrr@{}}
\toprule
Макро ST-RAE, среднее по четырём сидам & $\lambda=0$ & $\lambda=3$ & $\lambda=3$ + изо & $\lambda=0$ + изо \\
\midrule
& 0.781 & 0.810 & \textbf{0.747} & 0.773 \\
\bottomrule
\end{tabularx}
\caption{Четвёртый столбец --- контроль, без которого третий ничего не значил бы:
перекалибровка помогает обеим рукам, но рука с каналом после неё лучше руки без него,
и знак держится на всех четырёх сидах.}
\label{tab:joint}
\end{table}

Читать таблицу так. Сам по себе канал ($\lambda = 3$) хуже базовой линии. Сама по себе
перекалибровка ($\lambda = 0$ + изо) помогает, но умеренно. А вместе они дают лучший
результат из четырёх. Значит совместное правдоподобие переносит \emph{ранг}, а шкалу
переносит перекалибровка \emph{после} него; порознь не работает ни то, ни другое.

\emph{Возражение, которое пришлось проверить.} Две свободные головы над общим стволом ничем
друг с другом не связаны: голова скрининга вольна представлять $\log_2$fc как угодно, и
ничто не заставляет её выражать его через активность. Строго говоря, выше измерена
многозадачность, а не совместное правдоподобие в том виде, в каком его задаёт
раздел~\ref{sec:gen}, --- там скрининг связан с активностью \emph{фиксированной} кривой
прибора $g(\pi)$, подогнанной заранее (раздел~\ref{sec:calib}).

Одноголовый вариант это исправляет буквально: один латент, предсказание канала считается как
$g$ от предсказанной активности, ноль новых параметров, та же ручка. Результат обратен
ожиданию --- он \emph{хуже} двухголовой руки, и на этот раз знак держится на всех четырёх
сидах: до перекалибровки макро 1.000 против 0.810.

Первое объяснение напрашивалось само и не подтвердилось. Кривая прибора $g$
насыщается: у очень активных соединений сигнал упирается в потолок, у совсем неактивных --- в
пол, и в обеих зонах её производная почти нулевая. Там, где производная нулевая, показание
скрининга не может сказать про активность ничего: любое значение активности одинаково с ним
согласуется. Естественно предположить, что одноголовая рука страдает тем сильнее, чем больше у
фермента таких «слепых» соединений. У CYP3A4 их больше всех --- около сорока процентов
выборки.

Объяснение через насыщение при этом не проходит. Оно предсказывает, что хуже всех
будет там, где слепая доля больше, то есть на 3A4. Разложение потери по ферментам говорит
обратное:

\begin{table}[htbp]\centering\small
\begin{tabularx}{\textwidth}{@{}Xrrrr@{}}
\toprule
Потеря при $\lambda = 3$ против $\lambda = 0$, среднее по четырём сидам & 1A2 & 2C9 & 2D6 & 3A4 \\
\midrule
Доля «слепых» соединений (что предсказывает насыщение) & 0.22 & 0.07 & 0.14 & \textbf{0.40} \\
Двухголовая рука   & $+0.05$ & $-0.03$ & $+0.12$ & $-0.03$ \\
Одноголовая рука & $+0.24$ & $+0.01$ & $\mathbf{+0.63}$ & $+0.01$ \\
Она же при разрушенном канале & $+0.38$ & $+0.25$ & $+0.39$ & $+0.21$ \\
\bottomrule
\end{tabularx}
\caption{Самый слепой фермент оказался наименее пострадавшим. А последняя строка ломает и
всё информационное объяснение разом: канал там практически мёртв, а потеря выросла.}
\label{tab:dose}
\end{table}

CYP3A4 несёт сорок процентов слепых соединений и теряет меньше всех; CYP2D6 несёт четырнадцать
и теряет на порядок больше всех. Предсказанного насыщением порядка не наблюдается. То же и со
вторым объяснением --- поглощением промаха фиксированной кривой: величина, которая его меряет,
упорядочивает ферменты не в ту сторону.

Сказать «обе гипотезы исключены» при четырёх точках было бы слишком сильно, и раньше здесь
именно так и было сказано. Правильнее: предсказанного ими порядка не видно. Исключает их
другое измерение.

\subsection{Шумовая лестница: что она показала и что переписала}

Порядок из четырёх ферментов конфаундирован со всем сразу, и никакое количество сидов этого не
чинит. Чинит вмешательство: если испортить скрининговый канал управляемой дозой шума, то внутри
одного фермента не меняется ничего --- ни соединения, ни метки, ни полосы, ни разбиение, ни
начальные веса, --- кроме того, сколько канал знает. Четыре точки превращаются в четыре кривые.

Шум добавляется к стандартизованной цели канала, и сумма делится на $\sqrt{1+\eta^2}$, так что
разброс цели остаётся прежним, а падает ровно её связь с активностью --- в известное число раз.
Контроль: при $\lambda = 0$ канал в потери не входит, и предсказания при $\eta = 2$ обязаны
совпасть с предсказаниями при $\eta = 0$ побитово. Совпали, на всех четырёх сидах.

Внутрифермент\-ный отклик есть, и он воспроизводится. У двухголовой руки знак сдвига держится
на всех четырёх сидах для трёх ферментов из четырёх, при ошибке среднего в несколько тысячных;
у одноголовой --- для всех четырёх. Это единственная часть подраздела, где
вывод не опирается на сравнение ферментов между собой, и ради неё лестница и строилась.

Дальше начинается неожиданное. Порча одноголовой руки оказалась не информационной, и прежнее
объяснение --- «своей болезни у жёсткой калибровки нет, она впятеро умножает уже имеющуюся» ---
измерением снято. Если разрушить 95--97 процентов информации канала, потеря не исчезает, а
\emph{растёт}: на 1A2, 2C9 и 3A4 она увеличивается на $+0.15$, $+0.25$ и $+0.20$. Уцелевшие
проценты информации не могут делать больше работы, чем делали все сто. Значит то, что портит
жёсткую руку, живёт не в содержании канала.

У двухголовой руки поферментной структуры не обнаружено вовсе, и это отрицательный результат,
который стоит записать наравне с положительными. Средние отклонения от единой кривой не превышают 0.009 при собственном разбросе 0.033, и остаток
неотличим от того, что даёт случайная перестановка меток. У одноголовой та же структура
огромна. То есть всё поферментное локализовано в жёсткой связи, а не в канале.

Складывается из этого одномеханизменное чтение, и данные его держат. Канал тянет
предсказанную активность к псевдо-метке, полученной из скрининга. \emph{Качество} этой метки
задаёт шум --- отсюда монотонный отклик внутри фермента. А \emph{масштаб} тяги в единицах
$\pic$ задаёт прибор: у жёсткой связи он фиксирован калибровкой и от шума не зависит, у
свободной головы его нет вовсе, потому что голова вольна поглотить расхождение в себя. Отсюда
и поферментная структура там, где связь жёсткая, и её отсутствие там, где голова свободна.

Двухмеханизменное объяснение --- «канал несёт информацию, которую шум разрушает, плюс
многозадачное давление, которого шум не трогает» --- напрашивалось и не подтвердилось ни одной
своей половиной. Давления нет в той руке, где нет жёсткой связи, а в той, где оно есть, шум его
не сохраняет, а усиливает.

Всё сказанное ограничено одним обстоятельством, и снять его добавлением сидов нельзя. Чем
именно упорядочены ферменты, этот эксперимент не различает. По четырём ферментам совпадают, с попарным Спирменом ровно $\pm 1$,
три разные величины: связь скрининга с активностью; средняя ширина доверительной полосы, из
которой метрика и считает ошибку; и усиление калибровочной кривой --- сколько единиц $\pic$
ложной тяги производит одно стандартное отклонение остатка скрининга. Ни один тест этого
подраздела их не разделяет, и шумом разделить нельзя в принципе: шум двигает информативность,
а полосу и карту прибора не двигает.

Одну из трёх удалось вычеркнуть, и дёшево. Если пересчитать метрику по тем же сохранённым
предсказаниям, выровняв полосу на всех ферментах или убрав её вовсе, поферментная структура
почти не меняется: 2D6 остаётся худшим с огромным отрывом, 1A2 вторым, а 2C9 и 3A4 --- около
нуля. Значит эффект живёт в самих предсказаниях, а не в том, как метрика их читает, и ширина
полосы объяснением быть не может. Остаются две неразличимые: информативность скрининга и
усиление прибора. Первая привлекательнее, но не доказана.

Разделить их может ровно одно вмешательство, и оно дешёвое: поменять местами калибровочные
константы $(E, h)$ между CYP2D6 и CYP3A4 и прогнать одноголовую руку заново. Данные, метки,
полосы, разбиение, инициализация и связь скрининга с активностью при этом остаются буквально
теми же, двигается только карта прибора. Оно запущено, и ответ отрицательный: карта порядок
не задаёт.

Прогон снабжён двумя контролями, и оба сработали. При $\lambda = 0$ карта не вызывается
вовсе, и предсказания совпали с необменянными побитово на всех четырёх сидах, то есть обмен
не достаёт туда, куда не должен. А два необменянных фермента, 1A2 и 2C9, сохранили свои
карты и показывают, сколько шума даёт сам перезапуск: 0.019 и 0.009. Сдвиг у обменянных надо
читать против этой величины, а не против нуля.

\begin{table}[htbp]\centering\small
\begin{tabularx}{\textwidth}{@{}Xrrrr@{}}
\toprule
Сдвиг порчи после обмена карт, по сидам & 1A2 & 2C9 & \textbf{2D6} & \textbf{3A4} \\
\midrule
Чья теперь карта      & своя & своя & 3A4 & 2D6 \\
Сид 0                 & $-0.02$ & $+0.00$ & $\mathbf{+0.61}$ & $+0.00$ \\
Сиды 1--3, среднее    & $+0.02$ & $-0.01$ & $+0.00$ & $+0.02$ \\
\bottomrule
\end{tabularx}
\caption{Обменянные ферменты сдвинулись меньше, чем нетронутые контрольные.}
\label{tab:swap}
\end{table}

Предсказание было такое: 2D6, получив более крутую карту 3A4, должен пострадать заметно
меньше своих $+0.63$, а 3A4 с пологой картой 2D6 --- уехать с почти нуля в десятые. Не
произошло ни того ни другого. Без первого сида 2D6 сдвинулся на $+0.003$, а 3A4 на $+0.016$,
то есть \emph{меньше}, чем сдвинулся контрольный 1A2, которому вообще ничего не меняли.
Порча осталась при своём ферменте: 2D6 по-прежнему худший с огромным отрывом, 3A4
по-прежнему около нуля.

Значит из двух неразличимых величин карта прибора выбывает, и выбывает по вмешательству, а
не по корреляции. Остаётся информативность скрининга --- или что-то ещё в данных самого
фермента, чего мы не назвали. Второе честнее: эксперимент показал, где механизма \emph{нет},
и не показал, где он есть.

\subsection{Виноват был не сид, а одно соединение}

Первый сид к этому моменту трижды создал вывод, которого на трёх остальных нет: сдвиг порчи
2D6 в таблице выше, идеальный порядок из четырёх точек и расхождение в \texttt{NaN}. Правило
четырёх сидов каждый раз это ловило, но диагноз ставило неверный --- «разбиение неудачное».
Разбиение тут ни при чём.

Само оно ничем не выделяется: размеры фолдов, средняя активность, разброс, доля активных и
знаменатель метрики у первого сида такие же, как у остальных трёх, вплоть до второго знака.
А вот предсказания выделяются, и резко. У двухголовой руки при $\lambda = 3$ на CYP2D6
находится \emph{одно} соединение, \texttt{OCNT-2328942} с истинной активностью 2.53, которому
модель предсказывает $-360$ на первом сиде и $-22$ на третьем; на двух других сидах оно ведёт
себя нормально. Показание скрининга у него при этом заурядное (отклонение полтора десятых
стандартных), так что это не выброс в данных, а срыв оптимизации, который приходится на него.

ST-RAE --- сумма модулей отклонений, поэтому одно предсказание в $-360$ добавляет к числителю
около 362 при знаменателе порядка 120. Одна молекула из 1493 утраивает метрику фермента.

Обрежем предсказания диапазоном меток фермента плюс-минус две единицы. Порог отсекает
невозможное и не трогает низкое: на трёх ферментах из четырёх он не задевает ни одного
соединения, на 2D6 --- в среднем полсоединения. Результат:

\begin{table}[htbp]\centering\small
\begin{tabularx}{\textwidth}{@{}Xrrrrr@{}}
\toprule
Порча 2D6 при $\lambda = 3$, по сидам & сид 0 & сид 1 & сид 2 & сид 3 & знак \\
\midrule
Двухголовая, как есть   & $+0.609$ & $-0.040$ & $-0.121$ & $+0.034$ & 2 из 4 \\
Двухголовая, обрезано   & $+0.035$ & $+0.027$ & $+0.027$ & $+0.034$ & \textbf{4 из 4} \\
\addlinespace[2pt]
Одноголовая, как есть   & $+0.941$ & $+0.536$ & $+0.425$ & $+0.613$ & 4 из 4 \\
Одноголовая, обрезано   & $+0.575$ & $+0.574$ & $+0.587$ & $+0.596$ & \textbf{4 из 4} \\
\bottomrule
\end{tabularx}
\caption{Обрезка невозможных значений превращает самое неустойчивое число раздела в одно из
самых устойчивых.}
\label{tab:clip}
\end{table}

Отсюда следуют две вещи, и они противоположны по знаку.

\emph{Утверждение про двухголовую руку не выживает.} Её порча на 2D6 равна не $+0.120$, а
$+0.031$, и после этого 2D6 перестаёт быть худшим ферментом --- худшим становится 1A2 с
$+0.047$. Идеальный порядок, на котором стоит весь предыдущий подраздел, был создан одним
соединением, попавшим на два сида из четырёх; без него ранговое согласие с информативностью
скрининга равно $-0.800$, а не $-1.000$. Фразу «сильнее всего канал вредит на 2D6, том самом
ферменте, вокруг которого построен документ» надо снять: на двухголовой руке она держалась на
одной молекуле.

\emph{Утверждение про одноголовую руку выживает, и становится крепче.} Её порча на 2D6 равна
$+0.583$ против $+0.235$ у 1A2 --- разрыв в два с половиной раза остаётся, порядок ферментов
не меняется, ранговое согласие как было $-1.000$, так и осталось. Больше того, обрезка её
\emph{стабилизирует}: разброс по сидам падает с 0.516 до 0.022, то есть в двадцать три раза.
Всё, что раздел говорит про жёсткую связь, посчитано теперь на числах, которые не зависят от
того, какой сид взять.

\emph{И то, чего это не касается.} Отправляемая модель --- бустинг, а не сеть, и он за
пределы диапазона меток не экстраполирует: все 750 предсказаний в
\texttt{results/submission/} лежат внутри. Обрезка там не нужна. Но \texttt{src/submit.py}
её и не делает, так что если отправлять когда-нибудь станет сеть, защиты от $-360$ в
конвейере нет.

Помельче, но записать стоит. Две ячейки одноголовой руки получены после численного спасения: без ограничения на показатель экспоненты в калибровочной кривой она разошлась в
\texttt{NaN} на первом сиде. Ограничение нейтрально --- все конечные ячейки воспроизвелись до
последнего знака, --- но сам факт стоит записать: жёсткая рука не просто хуже, она способна
не обучиться там, где свободная работает нормально, на том же сиде и с теми же начальными
весами."""

Стоит спросить и о чистоте самого измерительного прибора. Изотоника монотонна, поэтому внутри одного
фолда порядок она сохраняет точно --- проверено, ни одной перестановки на двадцати
применениях. Но у неё есть свойство, которое легко проглядеть: она склеивает соседние
предсказания в одно значение. Сотни соединений фолда отображаются в несколько десятков
различных чисел, и в склеенном значении оказывается больше девяноста пяти процентов
соединений. А поскольку каждый фолд получает свою карту, на склеенном из пяти фолдов векторе
порядок уже не сохраняется строго: несколько процентов пар меняются местами, и худшее
соединение уезжает на сотню с лишним позиций списка. Хуже того, ранговая корреляция с самими
метками --- то, что прибор обязан не трогать, --- слегка падает на всех четырёх ферментах.

Сравнение двух рук это всё же не отменяет, и по конкретной причине: искажение почти синфазно.
Оно примерно одинаково бьёт по обеим рукам, поэтому \emph{разность} между ними смещается на
порядок меньше, чем каждая из них по отдельности. Пользоваться прибором можно; говорить, что
он сохраняет ранг, нельзя.

Остаётся вопрос, шум ли разрыв между руками после перекалибровки. После изотоники руки почти
сходятся --- 0.747 у двухголовой руки против 0.752 у жёсткой. Разница 0.006 сначала была
названа «вдвое меньшей, чем разброс по сидам», и это была ошибка выбора линейки: разброс
по сидам брался \emph{до} перекалибровки. Но обе руки к этому моменту уже перекалиброваны,
а перекалибровка разброс схлопывает почти в десять раз. Против правильной линейки разрыв ---
это два стандартных отклонения, и знак его держится на всех четырёх сидах.

Формулировка поэтому такая: форма связи между каналами стоит 0.006 ранговой информации ---
мало, но измеримо. Не «в пределах шума». Разница принципиальна: второе закрывает вопрос,
первое --- это то же самое утверждение, что и до перекалибровки, только ослабленное в
десятки раз.

Отсюда следует, во что обходится интерпретируемость. Прежний вывод, что спорить о форме
связи между каналами было бы спором не о том, верен про количество информации и неверен как
итог:
потому что читается как оправдание раздела~\ref{sec:gen}. Наоборот: рука, которая буквально
реализует его порождающую конструкцию, --- это та самая рука, которой нужна самая сильная
починка. Честная рамка другая. Фиксированная связь есть моделирующее допущение, которое
\emph{стоит} производительности и \emph{покупает} интерпретируемость, и у этого есть ценник:
до перекалибровки 1.000 против 0.810, после --- 0.752 против 0.747.

\emph{Происхождение этих чисел.} Первая версия обеих рук в репозитории не сохранилась:
скрипт писал оба режима в один файл под ключом, в котором режима не было, и второй прогон
затирал первый. Ключ исправлен, обе руки пересчитаны заново на четырёх сидах, и всё, на что
опирается подраздел, воспроизвелось. Тождество при $\lambda = 0$ проверено буквально:
максимум модуля разности предсказаний двух рук равен нулю на всех четырёх сидах, как и
должно быть по построению.

\subsection{Сеть против бустинга: разница меньше, чем починка}

Остаётся сравнение нейросетевой модели с бустингом --- единственное место раздела, которое
влияет на то, что мы отправляем. Раньше оно было посчитано на одном сиде разбиения, и притом
на том самом, куда попадает срывающееся соединение из предыдущего подраздела. То есть число,
на которое опирается решение об отправке, было получено на единственном заведомо испорченном
прогоне. Пересчитано на четырёх сидах, с обрезкой предсказаний сети и с одинаковой
постобработкой на обеих сторонах.

\begin{table}[htbp]\centering\small
\begin{tabularx}{\textwidth}{@{}Xrrrr@{}}
\toprule
Макро ST-RAE, среднее по четырём сидам & $\delta = 0$ & $\delta = 0.2$ & $\delta = 0.4$ & $\delta = 0.6$ \\
\midrule
Бустинг, без постобработки        & 0.767 & 0.828 & 0.897 & 0.972 \\
Бустинг, аффинная пара            & \textbf{0.716} & 0.761 & 0.828 & 0.913 \\
Сеть, аффинная пара               & \textbf{0.715} & 0.763 & 0.832 & 0.918 \\
\bottomrule
\end{tabularx}
\caption{Разница между моделями --- в четвёртом знаке. Разница между «с постобработкой» и
«без» --- в втором.}
\label{tab:vsboost}
\end{table}

Прежний вывод был «бустинг впереди, но разрыв сужается вдвое». Он не выживает целиком.
Разрыв оказался не 0.089, а от 0.000 до 0.005 --- на два порядка меньше, --- и он не
сужается, а растёт: при $\delta = 0$ модели неразличимы, а к $\delta = 0.6$ бустинг уходит
вперёд на 0.005. Направление противоположно ожидавшемуся.

Измеренным при этом можно назвать не всё. При $\delta$ ниже 0.3 знак разности не держится на
четырёх сидах, то есть там модели попросту неразличимы. Знак держится от 0.3 до 0.6, и там
бустинг впереди, но на величину, которую стоит сравнить с чем-нибудь: сама постобработка
стоит 0.051, то есть \emph{вдесятеро больше всей разницы между моделями}. Отношение
уточнено в подразделе~\ref{sec:rawvspost}, и уточнение оказалось сильнее: постобработка не
добавляется к прочим улучшениям, она их замещает.

Отсюда практический вывод, и он не про архитектуру. Выбор между бустингом и сетью в этом
диапазоне почти ничего не решает; решает то, как обработаны предсказания после модели, и
при каком $\delta$ эта обработка подогнана. Строить дальше стоит второе, а не первое.

Сюда же результаты из предыдущих разделов. Отрицательные: решающий слой на пуле границ имеет потолок
0.015 макро и в реалистичной версии теряет 0.018; выигрыш механистического блока по ST-RAE неотличим
от нуля и поштучно, и на макро. Положительный, найденный позже и описанный
в разделе~\ref{sec:decision}: усадка предсказаний к подогнанной аффинной паре даёт $-0.052$ макро
(0.7673 до 0.7150), на порядок больше механистического блока. Выигрыш живёт в геометрии метрики,
а не в качестве предсказания, и это ограничение остаётся. А вот опасение, что на тестовой
маргинали он сменит знак, измерением снято: сдвиг там втрое слабее того, на котором знак
менялся.

\subsection{Представление и данные: два эксперимента, которых не было}

Весь этот раздел, как и вывод «выбор модели стоит 0.005, а постобработка 0.051», получен
внутри одного представления. Абляционная сетка --- это шесть наборов из счётчиков Моргана,
дескрипторов RDKit и механистического блока; ни предобученного энкодера из
раздела~\ref{sec:arch}, ни внешних данных в ней нет ни в одной строке. Утверждение было
локальным, а прочитано как общее, и это стоило проверить.

\emph{Энкодер: сначала отрицательный, потом наоборот.} Организаторы выложили шестнадцать
чекпойнтов chemprop, каждый предобучен предсказывать своё семейство дескрипторов. Взят
\texttt{rdkit2d}: он предобучен ровно на тех дескрипторах, что лежат в нашем блоке.
Эмбеддинг подан тому же ученику, на тех же фолдах, с той же метрикой.

\begin{table}[htbp]\centering\small
\begin{tabularx}{0.8\textwidth}{@{}Xr@{}}
\toprule
Представление & Макро ST-RAE \\
\midrule
FP+DESC+MECH, пересчитано здесь & 0.7673 \\
Эмбеддинг один & 0.7784 \\
Эмбеддинг + механистический блок & 0.7765 \\
FP+DESC+MECH + эмбеддинг & \textbf{0.7589} \\
\bottomrule
\end{tabularx}
\caption{Как \emph{замена} представление проигрывает 0.009 --- третья строка против первой,
оба набора с механистическим блоком; как \emph{добавка} --- выигрывает 0.008. Контроль
воспроизвёлся точно: 0.7673 против 0.7673 в сводке.}
\label{tab:emb}
\end{table}

Первые три строки были посчитаны сразу, четвёртая --- через сутки, и без неё вывод был
сформулирован неверно. Мы написали «предобученное представление проигрывает рукодельному,
значит представление не было узким местом». Первая половина верна, вторая из неё не следует,
и \texttt{rdkit2d} --- ровно тот чекпойнт, на котором эта подмена особенно соблазнительна.
Он предобучен воспроизводить наши же дескрипторы, поэтому он самый честный выбор для вопроса
«бьёт ли выученное представление рукодельное» и самый \emph{невыгодный} для вопроса «добавляет
ли оно хоть что-нибудь»: энкодер, обученный повторять то, что у нас уже есть, меньше всех
способен что-то к этому добавить. Проигрыш замены не закрывает вопрос о дополнительности ---
его закрывает только строка с конкатенацией, и она даёт $-0.008$.

Поферментно добавка помогает 1A2 ($-0.022$) и 2D6 ($-0.039$) и вредит 2C9 ($+0.009$)
и 3A4 ($+0.019$) --- тот же рисунок, что и у внешних данных ниже.

\emph{Внешние данные: положительный, и не там, где ожидалось.} Вместе с базовой моделью
организаторы выложили её обучающий набор --- 8068 соединений с $\pic$ по нашим же четырём
ферментам, curated из ChEMBL. Две проверки сделаны до всего остального: пересечение
с \emph{тестом} равно нулю из 750, пересечение с обучением --- 64 соединения, они отброшены.

Нулевое пересечение с тестом стоит прочитать осторожнее, чем оно выглядит. Доля пересечения
с обучением --- 1.3\%, что на 750 тестовых соединениях предсказывало бы около десяти
совпадений; вероятность ровно нуля при биномиальной модели $5\cdot10^{-5}$. Ноль поставлен
намеренно, и объяснений два. Либо тест собирали в обход публичных данных --- тогда он обеднён
публичной химией и модель «тест = якоря плюс их аналоги из того же пула», на которой стоит
раздел~\ref{sec:trans}, слабеет. Либо внешний файл почистили против теста перед публикацией
--- тогда про состав теста это не говорит ничего. Первая гипотеза требует, чтобы тест лежал
\emph{дальше} от публичной химии, чем наше обучение; измерено обратное, тест ближе на
$+0.014$ медианы, интервал $[+0.010, +0.021]$ с ресемплингом обеих сторон. Гипотеза
отвергнута. Заодно закрыта утечка по близости, а не только по совпадению: ни одно тестовое
соединение не имеет во внешнем наборе аналога с Танимото выше 0.7.

Шкалы у двух наборов не совпадают. На общих соединениях внешние метки выше на
$+0.22$ / $+0.35$ / $+0.60$ / $+0.56$, а по маргиналям разрыв доходит до $+1.36$ на 3A4.
Естественно было счесть это ошибкой калибровки и поправить вычитанием. Измерение говорит
обратное.

\begin{table}[htbp]\centering\small
\begin{tabularx}{\textwidth}{@{}Xrrrrr@{}}
\toprule
Изменение к контролю (отрицательное лучше) & 1A2 & 2C9 & 2D6 & 3A4 & Макро \\
\midrule
Внешние как есть        & $-0.026$ & $+0.022$ & $-0.036$ & $-0.011$ & $-0.013$ \\
Сдвиг по парным         & $-0.019$ & $+0.028$ & $-0.041$ & $+0.003$ & $-0.007$ \\
Сдвиг по маргинали      & $-0.009$ & $+0.082$ & $-0.043$ & $+0.116$ & $+0.037$ \\
\midrule
Столбец-индикатор источника & $-0.024$ & $\mathbf{-0.000}$ & $-0.042$ & $-0.002$ & $-0.017$ \\
Наклон к парному сдвигу     & $-0.029$ & $-0.007$ & $\mathbf{-0.071}$ & $-0.017$ & $\mathbf{-0.031}$ \\
\bottomrule
\end{tabularx}
\caption{Вычитание константы делает хуже; поправка, допускающая неоднородность, --- лучше.}
\label{tab:ext}
\end{table}

Верхние три строки читаются так. Видимый разрыв шкал --- по большей части не ошибка
калибровки, а настоящее обогащение активными: в ChEMBL публикуют то, что сработало. Сдвигая
внешние метки вниз, мы портим верные значения, и чем грубее оценка сдвига, тем сильнее.
Поправка по маргинали, самая грубая, стоит $+0.037$ вместо выигрыша.

Нижние две показывают, что инструмент был не тот. Разрыв --- это эффект \emph{отбора}, а
отбор не действует одинаково по всему химическому пространству, поэтому одно число на фермент
не может его выразить. Это та же ошибка, которую раздел~\ref{sec:trans} уже разбирал для
теста: сдвиг --- не вся форма. Столбец-индикатор источника (единица у внешних строк, ноль у
наших; предсказываем при нуле) даёт модели выразить поправку самой, по областям. Наклон ---
экспоненциальное перевзвешивание внешних меток к парному сдвигу --- не трогает ни одной метки
вовсе, только гасит перепредставленных активных.

Индикатор был предрегистрирован как строгое обобщение обеих крайностей: расщепившись по нему
в корне, дерево воспроизводит «без внешних», ни разу не расщепившись --- «как есть».
Единственный информативный исход --- лечь ниже обеих, и он выпал.

\emph{Про CYP2C9 первое объяснение было неверным.} Мы написали, что ему вредит поправка,
потому что у него меньше всего парных соединений, шесть, и сдвиг известен хуже прочих. Но
2C9 портится и в руке «как есть», где никакой поправки не применяется по построению, и
внешних меток у него 2506 --- вторые по количеству. Ни тонкость оценки, ни дефицит данных не
годятся. Обучение на одних внешних метках с предсказанием наших соединений отделяет их данные
от нашего объединения: по рангу 2C9 оказывается вторым из четырёх, то есть его внешние метки
не хуже прочих. Причина осталась не установленной; индикатор источника её чинит
($+0.022 \to -0.000$), но какой именно механизм он при этом снимает, из четырёх точек не
выводится.

\subsection{Все эти числа измерены не там, где считается результат}
\label{sec:rawvspost}

У предыдущего подраздела есть оговорка, которая при проверке оказалась не оговоркой, а
поправкой ко всему разделу. Каждое число абляционной сетки --- это ST-RAE \emph{сырых}
предсказаний вне фолда. А отправляем мы не их: перед отправкой к предсказаниям каждого фолда
подгоняется аффинная пара (раздел~\ref{sec:decision}), и она стоит 0.051 макро. Вопрос,
который следовало задать два месяца назад: что от измеренных выигрышей остаётся, если
сравнивать не сырые предсказания, а те, которые уходят в файл.

\begin{table}[htbp]\centering\small
\begin{tabularx}{\textwidth}{@{}Xrrrr@{}}
\toprule
Сид 0 & Сырой & После пары & Выигрыш сырой & \textbf{Выигрыш после} \\
\midrule
База (L2, FP+DESC+MECH)      & 0.7673 & 0.7150 & --- & --- \\
L1 вместо L2                 & 0.7218 & 0.7131 & $-0.046$ & $\mathbf{-0.002}$ \\
Столбец-индикатор источника  & 0.7504 & 0.7117 & $-0.017$ & $\mathbf{-0.003}$ \\
Наклон к парному сдвигу      & 0.7363 & 0.7156 & $-0.031$ & $\mathbf{+0.001}$ \\
Внешние строки как есть      & 0.7545 & 0.7218 & $-0.013$ & $\mathbf{+0.007}$ \\
Эмбеддинг конкатенацией      & 0.7589 & 0.7263 & $-0.008$ & $\mathbf{+0.011}$ \\
\bottomrule
\end{tabularx}
\caption{Аффинная пара подгоняется вне фолда, отдельно для каждой руки. Ни один из пяти
выигрышей её не переживает.}
\label{tab:rawpost}
\end{table}

Ни один не выживает. Все пять уходят под шумовой пол 0.007, а три из пяти меняют знак на
вредный. Крупнейшая находка --- смена квадратичной ошибки на абсолютную, $-0.046$ сырым, ---
после пары стоит 0.002.

Механизм виден, как только сказан. Аффинная пара --- это усадка, подобранная \emph{прямо под
метрику}, пофолдово. Всё, что снижает разброс предсказаний или снимает систематическое
смещение, она уже делает, и делает адаптивно. Отсюда и величина самой пары: на базе L2 она
стоит 0.052, на предсказаниях L1 --- всего 0.009, потому что L1 забрал почти всё, что там
было.

Квадрат $2\times2$ показывает то же самое с другой стороны, и заодно отвечает на естественный
вопрос, складываются ли поправки.

\begin{table}[htbp]\centering\small
\begin{tabularx}{0.85\textwidth}{@{}Xrr@{}}
\toprule
Сид 0 & Сырой & После пары \\
\midrule
L2                        & 0.7673 & 0.7150 \\
L2 + наклон к парному     & 0.7363 & 0.7156 \\
L1                        & 0.7218 & 0.7131 \\
L1 + наклон к парному     & 0.7225 & 0.7238 \\
\bottomrule
\end{tabularx}
\caption{Наклон стоит $-0.031$ поверх L2 и $+0.001$ поверх L1. Сложение предсказывало 0.691.}
\label{tab:square}
\end{table}

Не складываются. Три вмешательства --- согласованные с метрикой потери, перевзвешивание
внешних меток и аффинная усадка --- оказываются \emph{взаимозаменяемыми}, а не
дополняющими: любое одно доводит примерно до 0.713, а два вместе дают 0.724, то есть хуже
любого поодиночке. Сырая колонка растянута на 0.046, обработанная --- на 0.011.

Единственная рука проверена на четырёх сидах, и правило репозитория отвечает на вопрос само.
Сырой выигрыш L1 держит знак на всех четырёх и лежит между 0.041 и 0.046 --- это результат.
После пары он равен 0.002, и на третьем сиде знак переворачивается --- это не результат.

\emph{Одно исключение, и оно нашлось потому, что таблица выше неполна.} В неё не попал ствол
из подраздела про совместное правдоподобие --- он измерен в другом файле и записан сырым,
$-0.018$. Прогнанный через тот же конвейер, с той же обрезкой предсказаний сети, что и в
таблице~\ref{tab:vsboost}, и с одинаковой аффинной парой на всех трёх руках:

\begin{table}[htbp]\centering\small
\begin{tabularx}{\textwidth}{@{}Xrrrrr@{}}
\toprule
Сид & Ствол $\lambda=0$ & Ствол $\lambda=3$ & Бустинг & Канал & Ствол минус бустинг \\
\midrule
0 & 0.7397 & 0.7182 & 0.7150 & $-0.021$ & $+0.003$ \\
1 & 0.7446 & 0.7142 & 0.7183 & $-0.031$ & $-0.004$ \\
2 & 0.7376 & 0.7145 & 0.7164 & $-0.023$ & $-0.002$ \\
3 & 0.7435 & 0.7128 & 0.7124 & $-0.031$ & $+0.000$ \\
\midrule
Среднее & 0.7414 & \textbf{0.7149} & 0.7155 & $\mathbf{-0.026}$ & $-0.001$ \\
\bottomrule
\end{tabularx}
\caption{Канал скрининга --- единственное вмешательство в этом репозитории, которое переживает
постобработку.}
\label{tab:trunkpost}
\end{table}

Канал стоит $-0.026$ \emph{после} пары, знак держится на всех четырёх сидах, $t = -10.87$,
$p = 0.002$. Обрезка здесь не косметика: без неё сид 0 даёт 0.9192 --- это то самое соединение
с предсказанием $-360$, и таблица~\ref{tab:vsboost} обрезает по той же причине.

Вторая половина, однако, не следует. Ствол с включённым каналом бустинг \emph{не бьёт}: 0.7149
против 0.7155, знак разности скачет по сидам, $t = -0.39$, $p = 0.72$. Паритет, а не
превосходство. Решение отложить ствол остаётся верным; неверным было списать канал как пустой.

\emph{Почему одни вмешательства постобработка съедает, а другие нет.} Ответ в одну строку, и
он же превращает весь этот подраздел из наблюдения в критерий. Аффинная пара монотонна;
изотоника, которой мы калибровали ствол, тоже. Монотонное отображение двигает масштаб и
положение и \emph{не может изменить порядок}. Значит вмешательство переживает постобработку
тогда и только тогда, когда добавляет \emph{ранговой} информации --- а это измеряется прямо, а
не выводится.

\begin{table}[htbp]\centering\small
\begin{tabularx}{\textwidth}{@{}Xrrrl@{}}
\toprule
Прирост ранговой связи с истиной, 4 сида & $\Delta\rho$ & $t$ & $p$ & Знаки \\
\midrule
Канал в стволе ($\lambda=3$ против $\lambda=0$) & $\mathbf{+0.0350}$ & 13.83 & 0.001 & $++++$ \\
L1 вместо L2 & $+0.0016$ & 0.97 & 0.405 & $-{+}{+}{+}$ \\
Ствол $\lambda=3$ против бустинга & $+0.0016$ & 0.53 & 0.632 & $-{+}{+}{+}$ \\
\bottomrule
\end{tabularx}
\caption{Сам ранг: ствол $\lambda=0$ --- 0.5297, ствол $\lambda=3$ --- 0.5646, бустинг ---
0.5630, L1 --- 0.5646.}
\label{tab:rank}
\end{table}

Это закрывает три вопроса сразу. Смена потерь схлопывается, потому что ранга не добавляет ---
причём не оттого, что L1 «просто иначе усадил»: ранговая связь между предсказаниями L1 и L2
равна 0.82--0.96, то есть порядок переставлен изрядно, и ни одна из перестановок не оказалась
информацией. Канал выживает, потому что даёт 0.035 ранга. А паритет ствола с бустингом
объясняется тем же: канал поднимает ствол ровно \emph{до} ранга, который у бустинга уже есть,
0.5646 против 0.5630, и не дальше.

Отсюда правило для всякой будущей абляции, и оно бесплатно: \textbf{печатать прирост ранговой
связи рядом с сырой метрикой}. Сырая колонка отвечает на вопрос «сдвинуло ли вмешательство
предсказания», а это не тот вопрос; ранговая --- на вопрос «сдвинуло ли туда, куда
постобработка не дотягивается». Оценка после изотоники --- тот же критерий в единицах самой
метрики и консервативная граница: изотоника охватывает более широкий класс монотонных
отображений, чем аффинная пара, поэтому что переживёт её, переживёт и пару.

\emph{Что здесь на самом деле сломалось.} Не пять экспериментов, а конвенция измерения.
Причём документ содержал обе сразу и этого не заметил: таблица~\ref{tab:vsboost} сравнивает
бустинг с сетью \emph{после} аффинной пары и потому честно показывает разницу в четвёртом
знаке, а вся абляционная сетка и оба эксперимента предыдущего подраздела --- до неё. Числа
«выбор модели стоит 0.005» и «внешние данные стоят 0.013» несравнимы между собой: первое
измерено после постобработки, второе до. Сравнивать их, как это делает подзаголовок
предыдущего подраздела, было ошибкой, и она наша.

Отсюда тезис раздела формулируется сильнее, чем раньше, и точнее, чем в первой редакции этого
подраздела. Было: постобработка стоит вдесятеро больше выбора модели. Написали сначала:
постобработка \emph{замещает} прочие улучшения. Верное: \textbf{согласованная с метрикой
монотонная постобработка ставит потолок всему, что не двигает ранг} --- и это не про то, что
улучшать больше нечего, а про то, где искать. Из шести измеренных вмешательств потолка
избежало одно, ровно то, у которого прирост ранга отличен от нуля.

\emph{Три оговорки, и ни одна не спасает руки.} Аффинная пара здесь подогнана при
$\delta = 0$; под апостериорным распределением $\delta$ разворот тот же --- L1 со своим
$\delta$ даёт 0.7764 против 0.7647 у базы. Избыточность на \emph{нашей} маргинали не
доказывает избыточности на сдвинутой тестовой --- но проверить это без тестовых меток нельзя,
а принять в свою пользу означало бы сделать ровно то, против чего написан весь этот раздел.
И наконец: работа со сдвигом $\delta$ постобработкой тоже не поглощается, но по третьей
причине --- не потому, что двигает ранг, а потому, что двигает \emph{цель}. Пересчёт правила
на L1-предсказаниях смещает только 2C9, с $+0.3$ до $+0.4$, а выигрыш поферментной подгонки
остаётся тем же, 0.047 против 0.049. Монотонная пара подгоняется под нашу маргиналь; сдвиг
меняет, под какую маргиналь её подгонять, и потому лежит вне того класса, для которого
доказан потолок.

Практическое следствие: \texttt{submit.py} по итогам всей этой работы не меняется ни в одной
строке.
